#!/bin/bash
rm -r water* 
clear
find *.cpp *.h -type f -print0 | xargs -0 sed -i "" -E "s/[[:space:]]*$//"
g++ -g -Wall -std=c++11 *.cpp -o water

if [ $? ]; then
	if [ "$1" == "debug" ]; then
		gdb water --command=gdb_config
	else
		./water -r "2 + 2 * 3" 
	fi
fi
